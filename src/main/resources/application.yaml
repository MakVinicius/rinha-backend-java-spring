spring:
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/rinhadb?createDatabaseIfNotExist=true&useTimezone=true&serverTimezone=UTC
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always
#server:
#  port:
#    - 9999s